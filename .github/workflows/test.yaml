name: WorkflowTest

on:
  push:
    branches:
      - mydev

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Build and Test
        run: |
          # Add your build and test commands here
